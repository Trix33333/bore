# requirements.txt — Dépendances du bot Discord communautaire
# Généré pour Python 3.10+

# Discord.py v2.3+ — Slash commands, Cogs, Views, Intents
discord.py>=2.3.2,<3.0.0

# Chargement des variables d'environnement depuis .env
python-dotenv>=1.0.0

# aiohttp est une dépendance de discord.py, on le fixe pour éviter des conflits
aiohttp>=3.9.0,<4.0.0
